<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detail Product</title>
    <style>
        .product-image {
            max-width: 300px;
            height: auto;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <h1><%= product.name %></h1>
    
    <img src="/<%= product.image %>" alt="<%= product.name %>" class="product-image">
    
    <ul>
        <li>Brand: <%= product.brand %></li>
        <li>Warna: <%= product.color %></li>
        <li>Harga: <%= product.price %></li>
        <li>
            Category:  <a href="/products?category=<%= product.category %>">
                    <%= product.category %></a> 
        </li>
        <li>Ditambahkan pada: <%= product.createdAt.toLocaleDateString('id-ID', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' }) %></li>
        <li>Diupdate pada: <%= product.updatedAt.toLocaleDateString('id-ID', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' }) %></li>
    </ul>
    <a href="/products">All Products</a>
    <a href="/products/<%= product._id %>/edit">Edit</a>
    <form action="/products/<%= product._id %>?_method=DELETE" method="post">
</body>
</html>